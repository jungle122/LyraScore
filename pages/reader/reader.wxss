/* --- 1. 基础布局修复 --- */
page {
  background-color: #ffffff;
  box-sizing: border-box; /* ✨ 核心修复：防止页面变宽 */
}

view, text, scroll-view {
  box-sizing: border-box; /* ✨ 让所有元素都乖乖听话 */
}

.container {
  padding: 40rpx;
  width: 100%;
  /* 防止左右晃动 */
  overflow-x: hidden; 
}

/* 顶部菜单按钮 */
.nav-bar {
  display: flex;
  justify-content: flex-end; /* 靠右对齐 */
  margin-bottom: 20rpx;
}

.menu-btn {
  padding: 10rpx 20rpx;
  background-color: #f0f0f0;
  border-radius: 30rpx;
}

/* 标题区 */
.header-area {
  margin-bottom: 60rpx;
  text-align: center; /* 居中显示更像歌谱 */
}

.title {
  font-size: 56rpx;
  font-weight: bold;
  color: #333;
  display: block;
  margin-bottom: 16rpx;
}

.artist {
  font-size: 32rpx;
  color: #666;
}

/* 六大金刚 (阅读版) */
.meta-section {
  margin-bottom: 60rpx;
}

.meta-row {
  display: flex;
  justify-content: space-between;
  margin-bottom: 24rpx;
}

.meta-item {
  width: 30%;
  background-color: #f9f9f9; /* 非常淡的灰 */
  border-radius: 12rpx;
  padding: 16rpx 0;
  display: flex;
  flex-direction: column; /* 上下排列：上面是标签，下面是值 */
  align-items: center;
}

.label {
  font-size: 22rpx;
  color: #999;
  margin-bottom: 4rpx;
}

.value {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
}

/* 纸质谱信息 */
.paper-info {
  font-size: 36rpx;
  color: #555;
  padding: 30rpx;
  background-color: #f0f0f0;
  border-radius: 16rpx;
  margin-bottom: 60rpx;
  text-align: center;
}
/* --- 2. 歌词区 (准备接受动态字体大小) --- */
.content-area {
  line-height: 2.0;
  color: #333;
  font-family: monospace;
  white-space: pre-wrap;
  display: block;
  padding-bottom: 100rpx;
  /* 注意：这里不写 font-size 了，改用内联样式控制 */
}

/* --- 3. ✨ 新增：自定义侧边抽屉样式 --- */

/* 遮罩层 (半透明黑底) */
.drawer-mask {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5); /* 50%透明度黑色 */
  z-index: 900; /* 保证在最上层 */
}

/* 抽屉本体 (白色面板) */
.drawer-panel {
  position: fixed;
  top: 0;
  right: 0; /* 贴着右边 */
  width: 60%; /* 占据屏幕 60% 宽度 */
  height: 100%;
  background-color: #ffffff;
  z-index: 1000;
  padding: 40rpx;
  box-shadow: -4rpx 0 10rpx rgba(0,0,0,0.1);
  
  /* 弹性布局，方便放按钮 */
  display: flex;
  flex-direction: column;
}

/* ... 前面的样式不变 ... */

/* --- 抽屉内部样式升级 --- */

.drawer-title {
  font-size: 40rpx;
  font-weight: bold;
  margin-bottom: 40rpx;
  margin-top: 20rpx;
}

.menu-group {
  margin-bottom: 20rpx;
}

.menu-item {
  padding: 24rpx 0;
  font-size: 34rpx;
  color: #333;
  display: flex;
  align-items: center;
}

.divider {
  height: 2rpx;
  background-color: #f5f5f5;
  margin: 30rpx 0;
}

/* 控制器区域 (字体 & 节拍器) */
.control-group {
  margin-bottom: 30rpx;
}

.control-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 10rpx;
}

.control-label {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
}

.control-sub-label {
  font-size: 28rpx;
  color: #666;
}

.control-value {
  font-size: 28rpx;
  color: #999;
  font-family: monospace;
}

/* 删除按钮 */
.delete {
  color: #ff4d4f;
  margin-top: auto; /* 推到底部 */
  margin-bottom: 20rpx;
}

/* ✨ 新增：BPM 输入框样式 */
.bpm-input {
  width: 120rpx; /* 足够放下 3 位数 */
  text-align: center;
  font-size: 28rpx;
  color: #333;
  font-family: monospace;
  background-color: #f0f0f0; /* 浅灰色背景，表示可编辑 */
  border-radius: 8rpx;
  padding: 5rpx 10rpx;
  /* 去掉小程序输入框默认的边框 */
  border: none; 
}